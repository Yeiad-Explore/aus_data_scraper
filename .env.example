# Browser Configuration
HEADLESS=true
BROWSER_TYPE=chromium

# Delays (seconds) - Anti-blocking
MIN_DELAY_SECONDS=3.0
MAX_DELAY_SECONDS=6.0

# Timeouts (milliseconds)
PAGE_LOAD_TIMEOUT=30000
NAVIGATION_TIMEOUT=60000

# LLM Configuration (Optional - for enrichment phase)
LLM_PROVIDER=anthropic  # "anthropic", "openai", or "azure"
LLM_API_KEY=  # Add your API key here (sk-ant-... for Anthropic, sk-... for OpenAI)
LLM_MODEL=claude-3-5-sonnet-20241022
LLM_TEMPERATURE=0.0

# Azure OpenAI Configuration (only if LLM_PROVIDER=azure)
AZURE_OPENAI_ENDPOINT=  # e.g., https://your-resource.cognitiveservices.azure.com/openai/v1
AZURE_OPENAI_DEPLOYMENT=  # e.g., gpt-5.1-chat

# Retry Configuration
MAX_RETRIES=1
